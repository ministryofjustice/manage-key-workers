import styled from 'styled-components'
import { SPACING, FONT_SIZE, BREAKPOINTS } from '@govuk-react/constants'

export const FilterStyled = styled('div')`
  display: flex;
  align-items: flex-end;
  margin-bottom: ${SPACING.SCALE_5} !important;
`
